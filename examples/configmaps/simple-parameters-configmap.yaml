apiVersion: v1
kind: ConfigMap
metadata:
  name: simple-parameters
  labels:
    # Note that this label is required for the informer to detect this ConfigMap.
    workflows.nholuongut.io/configmap-type: Parameter
data:
  msg: 'hello world'
